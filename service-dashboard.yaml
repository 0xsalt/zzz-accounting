# service-dashboard.yaml — ZZZ Accounting dev service
# See: ~/.claude/skills/ServiceDashboard/SKILL.md
service:
  name: zzz-accounting
  type: native-daemon
  description: "ZZZ Accounting — demo brochure site inspired by The Accountant"
  tier: 1
  build:
    skip: true
  deploy:
    port: auto
    health_check: /
  lifecycle: owned
  dev:
    entry_point: node_modules/.bin/next
    port: 8088
